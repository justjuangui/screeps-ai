export interface gState {

}

export interface gScreepState {
    harvested_energy?: boolean
    upgrade_controller?: boolean
    role?: string
    can_carry?: boolean
    can_move?: boolean
    can_work?: boolean
    creep_energy?: number
    full_carry?: boolean
    source_find?: boolean
    spawn_find?: boolean
    controller_find?: boolean
    is_near_of?: string
}
